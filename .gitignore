# Local environment — contains real API keys, bearer tokens, and DB path.
.env
.env.local

# Runtime logs. Contain phone numbers, verification codes, token prefixes.
logs/

# Stale local SQLite files from before the Postgres migration.
data/

# Build / editor / OS junk.
*.bak
*.tmp
*~
.DS_Store

# Go build artifacts from local testing.
/pingclaw-server
/server
/localtest
/hostedtest
/openclaw-mock
/output

# Test artifacts.
hostedtest.env
hostedtest.log
localtest.log
test_pingclaw.db
test_pingclaw.db-wal
test_pingclaw.db-shm
pingclaw.db
pingclaw.db-wal
pingclaw.db-shm
