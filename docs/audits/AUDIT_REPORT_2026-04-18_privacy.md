# Documentation Audit Report — Privacy Policy
Generated: 2026-04-18 | Commit: fe7f829

## Executive Summary
| Metric | Count |
|--------|-------|
| Documents scanned | 1 |
| Claims verified | 28 |
| Verified TRUE | 26 (93%) |
| **Verified FALSE / Incomplete** | **2 (7%)** |

## False / Incomplete Claims Requiring Fixes

### web/privacypolicy/content.md

| Line | Claim | Reality | Fix |
|------|-------|---------|-----|
| 33 | "Pairing tokens and API keys can be rotated at any time from your dashboard" | API keys: correct (dashboard has Rotate button). Pairing tokens: no dashboard UI exists — only regenerated by signing out and back in on the app. | Update to "API keys can be rotated from your dashboard. Pairing tokens are reissued when you sign in again on the app." |
| 50 | "These caches contain no data beyond what is already stored in the database" | Rate limit counters (`rl:ip:*`) are Redis-only counters not in Postgres. They contain IP addresses temporarily. | Add: "Rate limit counters (IP-based, 1-hour expiry) are also stored temporarily to prevent abuse." |

## Verified Claims (26 of 28)

| Line | Claim | Status |
|------|-------|--------|
| 5 | "sends your phone's GPS location" | ✓ Both iOS and Android apps send location |
| 9 | "receives only a unique, opaque identifier" | ✓ Identity struct has only Provider + Sub, no email |
| 9 | "does not request or store your email address" | ✓ Scopes are empty/openid-only; email column dropped from schema |
| 13 | "Only your most recent location is stored" | ✓ Redis SET overwrites previous value |
| 14 | "expires automatically after 24 hours" | ✓ locationTTL = 24 * time.Hour |
| 14 | "never written to a permanent database" | ✓ No Postgres INSERT for location; legacy table dropped |
| 16 | "accessible only through your own account" | ✓ GetLocation scoped by userID from auth context |
| 20 | "Does not sell, share, or provide your data to third parties" | ✓ No analytics, tracking, or external API calls |
| 22 | "Does not track you when sharing is off" | ✓ Both iOS and Android stop location updates |
| 29 | "used by the iOS or Android app" | ✓ Both apps exist and use pairing_token |
| 30 | "via MCP or ChatGPT" | ✓ MCP endpoint + OAuth flow both issue api_key |
| 33 | "stored as irreversible SHA-256 hashes" | ✓ hashToken() uses sha256.Sum256() |
| 33 | "plaintext is shown once at creation" | ✓ rotateToken returns plaintext, stores only hash |
| 37 | "secret is stored in plaintext" | ✓ user_webhooks stores secret as TEXT |
| 41 | Account deletion removes all listed items | ✓ CASCADE deletes + explicit Redis cleanup |
| 45 | Account data description | ✓ Matches users table schema |
| 46 | Sign-in identities description | ✓ Matches user_identities (no email column) |
| 47 | Token hashes description | ✓ Matches user_tokens schema |
| 48 | Webhook description | ✓ Matches user_webhooks schema |
| 49 | Location in Redis with 24h expiry | ✓ Confirmed |
| 50 | Auth cache 5-min expiry | ✓ authCacheTTL = 5 * time.Minute |
| 50 | Webhook cache 5-min expiry | ✓ webhookCacheTTL = 5 * time.Minute |
| 50 | Sign-in codes 5-min expiry | ✓ webCodeTTL = 5 * time.Minute |
| 52 | "does not durably store" IP/User-Agent | ✓ No INSERT includes IP; rate limits are Redis-only |

## Human Review Queue
- None — all issues are straightforward text updates
